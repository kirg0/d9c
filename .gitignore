# Build output
/d9c
/d9c.exe
*.exe
*.exe~
*.test

# Test / coverage artifacts
*.out
coverage.*

# Runtime data written next to the binary (saved hosts, compose backups)
/d9c-hosts.json
/*.tar.gz

# User-local config (lives next to the binary; machine-specific)
/d9c-config.yaml
/d9c-plugins.yaml

# Claude Code workspace (settings + memory) — not tracked by git
/.claude/

# Internal roadmap — kept local, not published
/PLANE.md

# OS / editor cruft
.DS_Store
Thumbs.db
